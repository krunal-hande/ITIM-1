nano docker.sh
bash docker.sh
docker run nginx
  sudo chmod 777 /var/run/docker.sock
  docker run nginx
nano index.html
  docker run --name abcd -d -p 8080:80 nginx
    docker ps -a //to see all images and contener
    docker run it ubantu //if we're using in terminal
    docker rmi 'imageid' //to remove image id
    docker stop 'imageid'
